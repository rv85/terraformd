pipeline {
    agent any
    stages {
        stage ("welcome") {
            steps {

                echo "this is test branch"

                echo "this is main branch"

            }
        }
    }
}
